# CI-specific requirements with minimal dependencies
# Basic dependencies
requests>=2.25.0
pydantic>=2.0.0
python-dotenv>=0.15.0

# Testing dependencies
pytest>=6.0.0
flake8>=3.8.0

# Everything else is excluded for CI testing 